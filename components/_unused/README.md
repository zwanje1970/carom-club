# 미사용/아카이브 컴포넌트

현재 앱에서 import되지 않는 파일들을 보관합니다.
필요 시 참고용으로만 사용하고, 재사용할 경우 `components/` 적절한 위치로 옮긴 뒤 import를 복구하세요.

- **intro/LogoAnimation.tsx** — 이전 인트로 애니메이션 (현재는 IntroOverlay 사용)
- **intro/LogoBalls.tsx** — 3공 로고 전용 컴포넌트 (현재는 LogoLink 사용)
