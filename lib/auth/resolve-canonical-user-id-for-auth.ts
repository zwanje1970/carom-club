import { resolveCanonicalUserIdForAuth } from "../server/platform-backing-store";

/**
 * 세션/actors user id를 대회 권한·createdBy 비교용으로 정규화. 구현: platform-backing-store.
 */
export { resolveCanonicalUserIdForAuth };
