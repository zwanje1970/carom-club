/**
 * 게시카드 HTML 스냅샷(html2canvas)·편집 미리보기 공통 픽셀 아트보드.
 * 메인(`/site`) 면은 이 DOM을 쓰지 않고 저장된 PNG만 표시한다.
 */
export const TOURNAMENT_CARD_ARTBOARD_WIDTH_PX = 440;
export const TOURNAMENT_CARD_ARTBOARD_HEIGHT_PX = 161;
