/**
 * 게시카드 HTML 스냅샷(html2canvas)·편집 미리보기·메인 텍스트 오버레이 좌표 공통 픽셀 아트보드.
 * 메인(`/site`) 면은 배경 PNG + (선택) 이 크기에 맞춘 HTML 오버레이.
 */
export const TOURNAMENT_CARD_ARTBOARD_WIDTH_PX = 440;
export const TOURNAMENT_CARD_ARTBOARD_HEIGHT_PX = 180;
